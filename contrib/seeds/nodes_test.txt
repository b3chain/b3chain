# B3Chain testnet seed nodes (port 18533)
#
# Maintained by hand. After editing, regenerate src/chainparamsseeds.h:
#
#   python3 contrib/seeds/generate-seeds.py contrib/seeds/ > src/chainparamsseeds.h
#
# Live operational seeds resolve via the DNS seed
# `testnet-seed.b3chain.org` configured in
# src/kernel/chainparams.cpp::CTestNetParams; the entries here are the
# fallback fixed-seed list used when DNS is unreachable.
#
# Phase 8a bootstrap (one operator-run seed; vps2/vps3 to be added when
# provisioned).
166.88.4.250:18533  # b3chain seed-1 (existing host, also runs nginx + miner + faucet + explorer)
