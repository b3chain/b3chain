<%- include('../partials/header') %>
<%- include('partials/dashnav') %>

<section>
  <h1>Workers</h1>
  <% if (workers.length === 0) { %>
    <p class="muted">No workers yet. Connect a miner with username <code><%= user.email %>.&lt;workerName&gt;</code>.</p>
  <% } else { %>
    <table>
      <thead><tr><th>Name</th><th>1h hashrate</th><th>Total shares</th><th>Last seen</th></tr></thead>
      <tbody>
      <% workers.forEach(w => { %>
        <tr>
          <td class="mono"><%= w.name %></td>
          <td><%= formatHashrate(Number(w.h_hour)) %></td>
          <td><%= w.shares_total %></td>
          <td class="muted"><%= w.last_seen_at || '—' %></td>
        </tr>
      <% }) %>
      </tbody>
    </table>
  <% } %>
</section>

<%
function formatHashrate(h) {
  if (h >= 1e15) return (h/1e15).toFixed(2) + ' PH/s';
  if (h >= 1e12) return (h/1e12).toFixed(2) + ' TH/s';
  if (h >= 1e9)  return (h/1e9).toFixed(2)  + ' GH/s';
  if (h >= 1e6)  return (h/1e6).toFixed(2)  + ' MH/s';
  if (h >= 1e3)  return (h/1e3).toFixed(2)  + ' kH/s';
  return h.toFixed(2) + ' H/s';
}
%>

<%- include('../partials/footer') %>
